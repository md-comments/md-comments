name: Staging Release (Pre-release)

on:
  push:
    branches:
      - 'release/**'

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      environment: 'Staging'
    secrets: inherit

  prerelease:
    name: Publish Pre-release
    needs: build
    runs-on: ubuntu-latest
    environment: Staging
    permissions:
      contents: write
    steps:
      - name: Download Build Artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: artifacts

      - name: Create Pre-release
        uses: softprops/action-gh-release@v3
        with:
          draft: false
          prerelease: true
          tag_name: v${{ needs.build.outputs.version }}-rc
          name: Release v${{ needs.build.outputs.version }}-rc (Staging Pre-release)
          files: |
            artifacts/**/*.vsix
            artifacts/obsidian-plugin.zip
            artifacts/chrome-extension.zip
