{
  "name": "md-comments-monorepo",
  "private": true,
  "license": "MIT",
  "scripts": {
    "copy-icons": "node scripts/copy-icons.js",
    "watch": "pnpm copy-icons && pnpm --filter md-preview-comments run watch",
    "watch:vscode": "pnpm copy-icons && pnpm --filter md-preview-comments run watch",
    "watch:obsidian": "pnpm --filter obsidian-md-comments run dev",
    "watch:chrome": "pnpm copy-icons && pnpm --filter chrome-md-comments run dev",
    "watch:starlight": "pnpm --filter @md-comments/starlight run dev",
    "watch:demo-astro": "pnpm --filter md-comments-demo-astro run dev",
    "build:vscode": "pnpm copy-icons && pnpm --filter md-preview-comments run compile",
    "build:obsidian": "pnpm --filter obsidian-md-comments run build",
    "build:chrome": "pnpm copy-icons && pnpm --filter chrome-md-comments run build",
    "build:starlight": "pnpm --filter @md-comments/starlight run build",
    "build:demo-astro": "pnpm --filter md-comments-demo-astro run build",
    "build:pages": "mkdir -p _site/demo-astro && cp -r website/* _site/ && cp -r demo-astro/dist/* _site/demo-astro/",
    "preview:pages": "pnpm build:demo-astro && pnpm build:pages && npx serve _site",
    "build": "pnpm copy-icons && pnpm --filter @md-comments/shared run build && pnpm --filter md-preview-comments run compile && pnpm --filter obsidian-md-comments run build && pnpm --filter chrome-md-comments run build && pnpm --filter @md-comments/starlight run build && pnpm --filter md-comments-demo-astro run build",
    "package:vscode": "pnpm copy-icons && pnpm --filter md-preview-comments run package",
    "lint": "eslint .",
    "format:check": "prettier --check \"**/*.{ts,js,json,css,md}\"",
    "format": "prettier --write \"**/*.{ts,js,json,css,md}\"",
    "typecheck": "tsc --noEmit && pnpm --recursive exec tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "@vitest/coverage-v8": "^2.0.5",
    "eslint": "^8.57.0",
    "eslint-plugin-security": "^3.0.1",
    "js-yaml": "^4.3.1",
    "@types/js-yaml": "^4.0.9",
    "playwright": "^1.62.1",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
